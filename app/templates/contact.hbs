<h1>Contact Page</h1>
{{input type="email" value=emailAddress class="form-control" placeholder="Please type your e-mail address." autofocus="autofocus"}} <br>

{{textarea class="form-control" placeholder="Your message. (At least 5 characters.)" rows="7" value=message}} <br>

<div class="col-xs-10 col-xs-offset-1 col-sm-offset-0 col-sm-4 col-md-3">
    <button class="btn btn-primary btn-lg btn-block" disabled={{isDisabled}} {{action 'sendMessage'}}>
        <span class="glyphicon glyphicon-envelope"></span>  Send message 
    </button>
</div>

<br><br><br><br>

{{#if responseMessage}}
      <div class="alert alert-success">{{responseMessage}}</div>
{{/if}}
